// Keep this file as a safe no-op hook until interceptor logic is implemented.
// The previous implementation referenced undefined variables and broke TypeScript builds.
export const setupApiInterceptor = () => {
  // Intentionally left blank.
};
